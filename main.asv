%%main
clc, clear, close all

FitFcn = @myFitness;
nvars = 2;
% lb=1;
% ub=14;
[T, fval] = ga(FitFcn,nvars)
%,[],[],[],[],lb,ub)
syms x
y = (((sin (sqrt (x.^2+x.^2) )).^2) - 0.5)/(1 + 0.001.*(x.^2+x.^2)).^2;
figure(1)
fplot(y,[-100 100])
plot()
